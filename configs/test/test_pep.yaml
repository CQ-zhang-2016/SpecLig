dataset:
  test:
    class: PeptideDataset
    mmap_dir: ./datasets/peptide/LNR/processed

dataloader:
  num_workers: 8
  batch_size: 8

n_samples: 100
n_cycles: 0

sample_opt:
  guidance_scale: 1.8
  max_grad_norm: 4.0
  temperature: 1.2